# Amazon Athena Screenshot Evidence

These screenshots document the final Amazon Athena deployment and validation
of the batch-processing results produced by PySpark on Amazon EMR.

## Final EMR execution

- Commit: `120dda8`
- EMR cluster: `j-2KIK1VQPJT200`
- EMR step: `s-09947463792EMAWECP0P`
- Total raw lines: 10,365,152
- Valid records: 10,365,077
- Invalid records: 75
- Rejection reason: `invalid_request`

All final CSV outputs were generated by the same EMR execution and uploaded to:

`s3://scp-speed-results-25186396/batch/`

## Screenshots

### 01_athena_database_tables_views.png

Shows the Athena database `scp_web_logs_25186396` with seven external tables
and two views.

### 02_athena_batch_total_validation.png

Reconciles the requests-per-endpoint, hourly traffic, error-rate,
status-code and batch-summary totals. Each aggregate contains 10,365,077
valid requests.

### 03_athena_baseline_long_endpoints.png

Demonstrates that long real-world endpoint strings were loaded successfully,
including endpoints up to 7,878 characters.

### 04_athena_baseline_schema_validation.png

Confirms 893,048 baseline rows, no empty endpoints, no null RPM values,
a minimum RPM of 1.0 and a maximum RPM of 79.0.

### 05_athena_top_endpoints.png

Shows the ten endpoints with the largest historical request counts.

### 06_athena_error_rates.png

Shows the endpoints with the largest numbers of error responses and their
historical error rates.

### 07_athena_traffic_by_hour.png

Shows the complete 24-hour traffic distribution.

### 08_athena_response_bytes_reconciliation.png

Reconciles 128,870,996,472 response bytes between the endpoint aggregate and
the batch summary.

### 09_athena_status_code_distribution.png

Shows the distribution of all 15 HTTP status codes. HTTP 200 accounts for
92.4241 percent of valid records.

### 10_athena_batch_health_summary.png

Shows the final batch health view with 10,365,077 valid records,
128,870,996,472 response bytes, 177,634 error responses and the overall
historical error rate.

### 11_athena_endpoint_historical_summary.png

Shows the combined historical endpoint view created from the batch aggregate
tables.
