# Batch Results

This folder is reserved for machine-generated outputs from the
PySpark and Amazon EMR batch layer.

Expected outputs include:

- requests per endpoint;
- error rates;
- traffic by hour;
- baseline requests per minute;
- data-quality counts and rejected-record information.
